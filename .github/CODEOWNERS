# Generated — managed centrally; do not edit here.
# Guardrail-minimal: only the release-please-managed changelogs, owned by the
# release team. Config and manifest stay unowned — promotions and counter
# reseeds are human edits there. No `*` fallback — pairs with the
# production-branch-guardrail ruleset (0 required approvals + code-owner
# review) so release PRs need explicit release-team approval while
# everything else auto-merges freely.
CHANGELOG.md @hop-top/release
